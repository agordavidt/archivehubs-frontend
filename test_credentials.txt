email:    john@archivehubs.dev
password: password123

Login with the seed user:

text
email:    john@archivehubs.dev
password: password123
→ redirects to home.html → feed loads.

Sign up a new user:

Click "Create Account"

Choose Individual → fill step 1 → fill step 2 → accept terms → submit

See "Account Created" panel

Open the browser console, run:

js
const { api } = await import('/js/core/api.js');
await api.verifyEmail('mock-token');
Go to login, sign in with the same credentials

After login, flushPendingProfile fires → /account/create-individual-account is called → sessionStorage/localStorage cleans up

Reload the page after login → session persists (mock stores userId in localStorage).

Logout — from console for now (wired properly when you build the account popup):

js
const { logout } = await import('/js/core/session.js');
await logout();
location.reload();
What's deliberately deferred
Verification page — I left the "click this fake link" dev step in the console. A dedicated verify-email.html page reading ?t$k3n= from the URL is a 20-line addition; skipping it keeps this slice focused.

Forgot/reset password pages — endpoints are in api.js, UI pages are untouched. Add them later with the same pattern.

Corporate firstName/lastName — the backend validator expects those fields. I'm passing the corporate name as firstName and empty lastName. Backend dev may want to adjust the validator to accept corporateName. Flag it.

CSRF — the backend doesn't appear to set a CSRF cookie, so I didn't add the header. If it lands later, add one line in request().

Ready for the navbar slice next — it consumes getUser() and finally gives you something visible to click "Log Out" from. Say the word.

